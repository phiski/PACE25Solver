Das Programm wurde in Java geschrieben und kann einfach ausgeführt werden. Mit System.in wird die .gr-Datei eingelesen und mit System.out wird die Lösung direkt ausgegeben, wobei in der ersten Zeile die Größe des berechneten Dominating Sets steht und die restlichen Zeilen jeweils einen Knoten aus dem Dominating Set darstellen.
